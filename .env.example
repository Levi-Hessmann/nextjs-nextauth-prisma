# MySQL database url
DATABASE_URL=''

# SMTP settings for sending emails
SMTP_HOST=
SMTP_USER=
SMTP_PASS=
SMTP_PORT=465
SMTP_MAIL=
SMTP_FROM='"Example"<hey@example.de>'

# Generate one here: https://generate-secret.vercel.app/32 (only required for localhost)
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:3000